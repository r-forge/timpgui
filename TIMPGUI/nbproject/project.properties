application.args=
application.title=TIMPGUI
application.vendor=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/TIMPGUI.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.-jri=../../../../../jri
file.reference.jfreechart-1.0.5-lib=../../My Projects/TIMP/jfreechart-1.0.5/lib
file.reference.Joris-JRI=../../../../../MinGW/home/Joris/JRI
file.reference.JRI.jar=../../../../../Program Files/R/R-2.4.1/library/rJava/jri/JRI.jar
file.reference.JRI.jar-1=../../../../../Program Files/R/R-2.5.0/library/rJava/jri/JRI.jar
file.reference.JRI_0.2-4-JRI=../JRI_0.2-4/JRI
file.reference.My_Projects-TIMP=../../My Projects/TIMP
includes=**
jar.compress=false
javac.classpath=\
    ${libs.absolutelayout.classpath}:\
    ${libs.swing-layout.classpath}:\
    ${libs.Jama.classpath}:\
    ${libs.JRI.classpath}:\
    ${libs.JCommon.classpath}:\
    ${libs.JFreeChart.classpath}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:unchecked
javac.deprecation=true
javac.source=1.5
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javadoc.additionalparam=
javadoc.author=true
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=true
javadoc.windowtitle=
jnlp.codebase.type=local
jnlp.codebase.url=file:/D:/Documents%20and%20Settings/Joris/My%20Documents/Java%20Projects/TIMPGUI/dist/
jnlp.enabled=false
jnlp.offline-allowed=false
jnlp.signed=false
# Property libs.absolutelayout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.absolutelayout.classpath=../../../../../Program Files/NetBeans 6.0/java1/modules/ext/AbsoluteLayout.jar
# Property libs.iPlots.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.iPlots.classpath=../../../../../Program Files/R/R-2.6.1/library/iplots/java/iplots.jar
# Property libs.Jama.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Jama.classpath=../Libaries/Jama/Jama-1.0.2.jar
# Property libs.JCommon.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JCommon.classpath=../Libaries/JCommon/jcommon-1.0.12/jcommon-1.0.12.jar
# Property libs.JFreeChart.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JFreeChart.classpath=../Libaries/JFreeChart/jfreechart-1.0.9/lib/jfreechart-1.0.9.jar;../Libaries/JFreeChart/jfreechart-1.0.9/lib/jfreechart-1.0.9-swt.jar;../Libaries/JFreeChart/jfreechart-1.0.9/lib/jfreechart-1.0.9-experimental.jar;../Libaries/JFreeChart/jfreechart-1.0.9/lib/jcommon-1.0.12.jar;../Libaries/JFreeChart/jfreechart-1.0.9/lib/junit.jar;../Libaries/JFreeChart/jfreechart-1.0.9/lib/itext-2.0.6.jar
# Property libs.JGR.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JGR.classpath=../../../../../Program Files/R/R-2.6.1/library/JGR/java/JGR.jar
# Property libs.JRI.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JRI.classpath=../../../../../Program Files/R/R-2.6.2/library/rJava/jri/JRI.jar
# Property libs.swing-app-framework.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.swing-app-framework.classpath=../../../../../Program Files/NetBeans 6.0/java1/modules/ext/appframework-1.0.3.jar;../../../../../Program Files/NetBeans 6.0/java1/modules/ext/swing-worker-1.1.jar
# Property libs.swing-layout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.swing-layout.classpath=../../../../../Program Files/NetBeans 6.0/platform7/modules/ext/swing-layout-1.0.3.jar
main.class=TIMPGUI.TIMPGUI
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
